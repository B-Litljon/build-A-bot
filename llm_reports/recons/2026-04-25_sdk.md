---
type: recon
date: 2026-04-25
time: unknown
agent: OpenCode (Kimi K2.6)
model: kimi-k2.6
trigger: Reconnaissance of the Build-A-Bot Factory SDK ahead of decoupling work
head: unknown
scope: read-only
imported_from: SDK_RECON_REPORT.md
---

# Build-A-Bot Factory SDK — Reconnaissance Report

**Date:** 2026-04-25  
**Branch:** `main`  
**Mode:** Read-only  
**Agent:** OpenCode (Kimi K2.6)

---

## 1. Git State

```text
$ git rev-parse --abbrev-ref HEAD
main

$ git log --oneline -10
1578573 Merge pull request #55 from B-Litljon/agentic-updates
d740e0d feat: implement dataset generation and model training modules for day trading targets
35b29c5 feat: Add day trading dataset harvester and target labeling for Universal Scalper V4.0
1c5a836 Refactor Build-A-Bot: Transition to Universal Scalper V3.4
51d48c9 Refactor code structure for improved readability and maintainability
c612cc1 Merge pull request #54 from B-Litljon/ml-factory-abstraction-5007372660788178709
d26864c Hotfix: Enforce buying power constraint in RiskManager
8512dad Hotfix: Enforce max notional position capping in RiskManager
fd5315f Merge pull request #53 from B-Litljon/ml-factory-abstraction-5007372660788178709
917a339 Refactor Model Training to Framework-Agnostic Interface

$ git status --short
(nothing to report — working tree clean)

$ git stash list
(nothing to report — no stashes)
```

---

## 2. SDK File Inventory

| Path | Exists | Lines | Last Modified |
|------|--------|-------|---------------|
| `run_factory.py` | Y | 59 | 2026-04-06 14:02:40.437670687 -0700 |
| `run_live.py` | Y | 92 | 2026-02-24 23:41:09.045545346 -0800 |
| `src/strategies/base.py` | **N** | — | — |
| `src/strategies/strategy.py` | Y | 54 | 2026-02-18 17:47:54.805097114 -0800 |
| `src/strategies/concrete_strategies/ml_factory_strategy.py` | Y | 43 | 2026-04-06 21:43:42.525026075 -0700 |
| `src/strategies/concrete_strategies/ml_strategy.py` | Y | 415 | 2026-04-06 21:43:42.525026075 -0700 |
| `src/execution/factory_orchestrator.py` | Y | 175 | 2026-04-07 14:31:35.680360995 -0700 |
| `src/execution/live_orchestrator.py` | Y | 2387 | 2026-04-06 18:41:27.976796275 -0700 |
| `src/execution/risk_manager.py` | Y | 60 | 2026-04-07 14:31:35.680360995 -0700 |
| `src/execution/enums.py` | **N** | — | — |
| `src/execution/__init__.py` | Y | 11 | 2026-04-06 12:47:53.983135221 -0700 |
| `src/data/feed.py` | Y | 166 | 2026-04-06 18:41:27.988882384 -0700 |
| `src/data/equity_feed.py` | **N** | — | — |
| `src/data/factory.py` | Y | 68 | 2026-02-16 19:43:00.172915495 -0800 |

---

## 3. Alpaca Enum Import Audit (the blast radius)

### `alpaca.trading.enums`
```text
src/data/discovery.py:5:from alpaca.trading.enums import AssetClass, AssetStatus
src/execution/factory_orchestrator.py:10:from alpaca.trading.enums import OrderSide, TimeInForce
src/execution/live_orchestrator.py:109:from alpaca.trading.enums import OrderSide, OrderType, TimeInForce
```

### `alpaca.common.enums`
```text
(no hits)
```

### `alpaca.trading.requests`
```text
src/data/discovery.py:4:from alpaca.trading.requests import GetAssetsRequest
src/execution/factory_orchestrator.py:9:from alpaca.trading.requests import MarketOrderRequest
src/execution/live_orchestrator.py:110:from alpaca.trading.requests import (
```

### `MarketOrderRequest | LimitOrderRequest | OrderSide | TimeInForce`
```text
src/execution/factory_orchestrator.py:9:from alpaca.trading.requests import MarketOrderRequest
src/execution/factory_orchestrator.py:10:from alpaca.trading.enums import OrderSide, TimeInForce
src/execution/factory_orchestrator.py:126:        req = MarketOrderRequest(
src/execution/factory_orchestrator.py:129:            side=OrderSide.BUY,
src/execution/factory_orchestrator.py:130:            time_in_force=TimeInForce.GTC
src/execution/factory_orchestrator.py:164:        req = MarketOrderRequest(
src/execution/factory_orchestrator.py:167:            side=OrderSide.SELL,
src/execution/factory_orchestrator.py:168:            time_in_force=TimeInForce.GTC
src/execution/live_orchestrator.py:109:from alpaca.trading.enums import OrderSide, OrderType, TimeInForce
src/execution/live_orchestrator.py:111:    MarketOrderRequest,
src/execution/live_orchestrator.py:1345:        All entries now use a simple ``MarketOrderRequest`` with
src/execution/live_orchestrator.py:1484:            tif = TimeInForce.GTC if is_crypto else TimeInForce.DAY
src/execution/live_orchestrator.py:1486:            order_request = MarketOrderRequest(
src/execution/live_orchestrator.py:1489:                side=OrderSide.BUY,
src/execution/live_orchestrator.py:1727:            order_request = MarketOrderRequest(
src/execution/live_orchestrator.py:1730:                side=OrderSide.SELL,
src/execution/live_orchestrator.py:1731:                time_in_force=TimeInForce.GTC,
```

---

## 4. ExecutionOrder Current Shape

**File `src/strategies/base.py` does not exist.**  
The abstract strategy base lives in **`src/strategies/strategy.py`** instead.

There is **no `ExecutionOrder` dataclass** anywhere in the codebase.  
The only order-parameter-like abstraction is `OrderParams`, imported as `from core.order_management import OrderParams` (see §7 — `core/order_management.py` itself is missing).

**Current `src/strategies/strategy.py` imports block & class:**
```python
import sys
import os
from abc import ABC, abstractmethod
from typing import List, Dict

from core.signal import Signal
from core.order_management import OrderParams
import talib
import pandas as pd
import numpy as np
import polars as pl

class Strategy(ABC):
    ...
    @abstractmethod
    def get_order_params(self) -> OrderParams:
        ...
```

---

## 5. FactoryOrchestrator Order-Construction Sites

All Alpaca object construction & enum references in `src/execution/factory_orchestrator.py`:

```text
7-import polars as pl
8:from alpaca.trading.client import TradingClient
9:from alpaca.trading.requests import MarketOrderRequest
10:from alpaca.trading.enums import OrderSide, TimeInForce
11-
12-from data.feed import MarketDataFeed
--
125-
126:        req = MarketOrderRequest(
127-            symbol=symbol,
128-            qty=qty,
129:            side=OrderSide.BUY,
130:            time_in_force=TimeInForce.GTC
131-        )
132-
--
163-        """Market close of a position."""
164:        req = MarketOrderRequest(
165-            symbol=symbol,
166-            qty=qty,
167:            side=OrderSide.SELL,
168:            time_in_force=TimeInForce.GTC
169-        )
170-        try:
```

---

## 6. Test State

```text
$ ls -la tests/
total 32
drwxr-xr-x.  2 tha_magick_man tha_magick_man 4096 Apr  6 14:02 .
drwxr-xr-x. 13 tha_magick_man tha_magick_man 4096 Apr 25 16:16 ..
-rw-r--r--.  1 tha_magick_man tha_magick_man 5065 Feb 18 17:34 test_live_simulation.py
-rw-r--r--.  1 tha_magick_man tha_magick_man 2541 Apr  6 12:48 test_order_management.py
-rw-r--r--.  1 tha_magick_man tha_magick_man 4509 Feb  7 17:30 test_strategy_logic.py
-rw-r--r--.  1 tha_magick_man tha_magick_man 2580 Apr  6 14:02 verify_warmup.py
```

Test files found:
- `tests/test_live_simulation.py`
- `tests/test_order_management.py`
- `tests/test_strategy_logic.py`
- `tests/verify_warmup.py`

**Pytest config:** `pyproject.toml`, `pytest.ini`, `setup.cfg`, and `setup.py` were **not found** in the project root.

---

## 7. Drift Report

### Critical Missing Modules
- **`src/core/order_management.py`** is imported by **7+ source files** (`strategy.py`, `ml_strategy.py`, `ml_factory_strategy.py`, `rsi_bbands.py`, `sma_crossover.py`, and `tests/test_order_management.py`) but **does not exist on disk**. This means the codebase is currently **unrunnable** from a clean checkout for any path that touches `OrderParams` or `OrderManager`.  
  *Impact for broker enum abstraction:* If you plan to introduce a broker-agnostic `ExecutionOrder` or `OrderParams`, you may actually be filling a void that already exists — the file was likely deleted or never committed.

### Missing Expected SDK Files
- `src/strategies/base.py` — referenced in the task brief but absent; `strategy.py` serves as the ABC base.
- `src/execution/enums.py` — referenced in the task brief but absent; no local enum module exists yet.
- `src/data/equity_feed.py` — referenced in the task brief but absent.

### Alpaca Surface Area
- **Factory path** (`factory_orchestrator.py`): only `MarketOrderRequest`, `OrderSide`, `TimeInForce`. Clean and small.
- **Live path** (`live_orchestrator.py`): ~2,387 lines, also imports `OrderType`, and has **many** `MarketOrderRequest` construction sites (lines ~1486, ~1727). This is the high-blast-radius file.
- **Data path** (`data/discovery.py`): imports `AssetClass`, `AssetStatus`, `GetAssetsRequest`. Consider whether asset-discovery enums are in scope for the abstraction or stay data-layer specific.

### Branches of Interest
Local branch **`sdk-decoupling`** exists and sounds directly related to this mission. Remote `origin/sdk-decoupling` also exists. Worth inspecting before starting new abstraction work.

### Duplicate / Confusion Risk
- `src/strategies/strategy.py` defines `Strategy(ABC)`.
- `src/strategies/strategy_factory.py` exists (1,378 bytes, not inspected in depth) — no class-name collision detected, but the naming overlap is worth noting.

### TODO / FIXME Audit
- Zero `TODO`, `FIXME`, or `XXX` comments found in the four files checked (`risk_manager.py`, `factory_orchestrator.py`, `feed.py`, and the non-existent `base.py`).

### RiskManager Notes
- `risk_manager.py` is pure math (SL/TP bracket + position sizing) and **contains zero broker-specific code**. It is already agnostic — a good boundary to preserve.

### Feed.py Notes
- `feed.py` is tightly coupled to **Alpaca Crypto** (`CryptoHistoricalDataClient`, `CryptoDataStream`, `CryptoBarsRequest`, `TimeFrame`, `TimeFrameUnit`). Any broker abstraction should probably treat the *data feed* as a separate concern from the *order enum* abstraction, but be aware that `feed.py` already uses Alpaca `TimeFrame` enums.

---

**End of Report**
